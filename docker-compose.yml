version: '3.7'

services:
  web:
    build: .
    command: gunicorn partnerweb_project.wsgi --bind 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - 8000:8000
    env_file:
      - .env
  redis:
    image: redis:latest
    container_name: partnerweb3_redis